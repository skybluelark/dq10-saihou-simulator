# スマホ版UIデザイン設計書 (v0.54)

作成日: 2026-07-08 / 最終更新: 2026-07-10 (v0.54)
関連文書: [SPEC.md](SPEC.md)(ゲーム仕様の正) / [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)(M5) / [ARCHITECTURE.md](ARCHITECTURE.md)(A8 モバイル移植戦略)

## 1. 位置づけ

- M5「スマホ版UIデザイン検討」の成果物をまとめる設計書。スマホアプリ版(W1)と計算機モード(W3)のUIの土台になる。
- ゲーム仕様の正は従来どおり SPEC.md。本書で確定したUI仕様のうちゲーム挙動に関わるものは SPEC §4.3 へ反映する。
- **本書の「表示項目の決定事項」(§5)は、計算機モードの仕様検討(別セッション)への入力を兼ねる**。共有UI部品の洗い出しは計算機モード検討時に §6 で行う。

## 2. 決定済みの前提(2026-07-08)

| # | 項目 | 決定 |
|---|---|---|
| P1 | 画面の向き | **縦持ち固定** |
| P2 | 素材 | **生成画像を利用**。差し替えを考慮し、パーツの単位(分割方針)を設計観点に含める(§4) |
| P3 | DQ10への見た目の寄せ | 寄せる必要はない。ただし**寄せた場合の見え方を確認したうえで判断**する(§4.3。権利面の線引きに注意) |
| P4 | 画面構成 | ブラウザ版は必要項目・操作性の検討を経ていないため**ゼロベースで表示項目の要不要から確認**する(§5) |
| P5 | 操作系 | **特技選択・マスタップの操作系はブラウザ版を踏襲**(特技を選択→マスをタップ(対象ハイライト)→再タップで実行。対象不要特技は即実行。特技未選択のマスタップは「ぬう」自動選択。SPEC §4.3) |
| P6 | UIの方向性 | 機能・リリース優先だったブラウザ版と異なり、**ボタン画像・背景等を用いたリッチな見た目**を志向(DEVELOPMENT_PLAN §2 W1) |

## 3. 画面前提の検討パターン(決定: 2026-07-08。S4・S5 のみモックで確認)

| # | 論点 | 一般的な考え方 | 決定 |
|---|---|---|---|
| S1 | 基準デザイン幅と下限 | 幅 360〜390dp を基準に設計し、下限を決める。事実上の下限は **360×640dp**(Android 普及帯)/ iOS は 375×667(iPhone SE) | **基準 390dp・下限 360dp** |
| S2 | アスペクト比の変動 | 同じ幅でも高さは 640〜900dp+ と大きく変わる。どの領域を伸縮させるかを決めるのが縦持ちUIの要点 | **固定要素はそのまま、余りの縦余白は画面下端に寄せる**(2026-07-10 変更: 当初の「上下の余白を伸ばして中央寄せ」では解像度によりヘッダーとぬいパワー行の間に隙間が生じたため、要素間には余白を入れず下端にまとめる) |
| S3 | セーフエリア | ノッチ・ホームインジケータ等を避ける safe area inset 対応。背景はセーフエリア外まで敷き、操作要素は内側 | **対応する**(背景全面・操作要素は内側) |
| S4 | タッチターゲット寸法 | 最小 44pt(iOS)/48dp(Android) | **モックで確認** |
| S5 | 素材解像度 | 生成画像は表示サイズの2〜3倍で用意し縮小表示 | **モックで確認** |
| S6 | OSフォントスケール | リッチUI(画像枠に文字)は固定が無難 | **固定**(追従しない) |
| S7 | タブレット | 中央寄せ+余白(レターボックス)か拡大か | **中央寄せ+余白**(専用レイアウトなし) |
| S8 | ダークモード | 背景画像主体のリッチUIならテーマ固定が一般的 | **固定**(1テーマ。ダークモード非対応) |
| S9 | キーボード表示時 | 入力欄が隠れない対策。入力を別画面に寄せると楽 | **キーボードが出る画面を限定する**。シード入力はスマホ版では不可(必要になれば管理者モードを用意しその場合のみ開放)。リプレイ入力はリプレイ開始時の専用画面のみ |
| S10 | 横持ちの扱い | 縦固定なら横向きはロック。iPad は縦固定にフルスクリーン指定が必要 | **縦固定。iPad はフルスクリーン指定で縦固定を維持** |

## 4. 素材方針(生成画像)

### 4.1 パーツ分割の設計観点(差し替え考慮)

素材は「画面一枚絵」ではなく**部品単位**で管理し、個別差し替え・状態差分・多解像度化に耐える構成とする。

- **背景**: 画面全体の背景(セーフエリア外まで敷く)。画面種別ごと(プレイ画面/結果画面など)
- **パネル枠**: 状態表示・ログ等の枠。**9-slice(周囲を固定し中央を伸縮)**で可変サイズに対応できる形が望ましい
- **ボタン**: 特技ボタン・汎用ボタン。通常/押下/無効/選択中の**状態差分**をセットで用意。文字は画像に焼き込まず**テキストを重ねる**(特技追加・文言変更・多言語化に耐える)
- **マス**: 布マスの下地(状態色: 白/黄/緑/赤 は下地差分 or 色調変化のどちらで表すか)。しつけ枠・発光・対象ハイライトは**重ねる装飾パーツ**として分離
- **アイコン・バッジ**: ぬいパワー、布特性、必殺チャージ、無我など
- **エフェクト**: 吹き出し(ダメージ/会心/回復)、必殺演出など(静止画+CSS/コードアニメーションか、連番/スプライトか)

命名・格納規約(assets/ 配下のディレクトリ構成、状態差分のサフィックス等)は画面設計の確定後に定める。

### 4.2 権利面の線引き

- DQ10 の**画像・ロゴ・フォント・スクリーンショットの流用は不可**(著作権)。生成画像でも「DQ10 の UI を忠実に再現したもの」は依拠性が認められ得るためリスクがある。
- 「木枠+布地風」「和風RPGメニュー調」のような**雰囲気のレベルで寄せる**のは一般に問題ない。固有のデザイン(DQ特有のウィンドウ枠・書体・配色の組合せの再現)は避ける。

### 4.3 DQ10寄せの見え方確認(P3)— 決定済み

モック第1版(テーマA=モダン/テーマB=DQ雰囲気寄せ)の比較により、**トーンB(DQ雰囲気寄せ: 紺ウィンドウ+金縁+羊皮紙調。固有デザインの再現はしない)で決定**(2026-07-08)。以降の素材生成プロンプトはトーンBを基準にする。

### 4.4 素材パーツ一覧(2026-07-08 起票。モックv2系から洗い出し)

サイズは「表示dp → 作成px(@2x。S5)」。**文字は画像に焼き込まない(例外なし)**(日本語文字の生成品質が低いため、テキストはすべてフォント合成。2026-07-08 確定)。優先度: P1=プレイ画面に必須 / P2=選択・結果画面 / P3=演出・その他。

| ID | 用途 | 表示サイズ(dp) | 作成(px) | 状態差分 | 9-slice | 優先 |
|---|---|---|---|---|---|---|
| bg_main | 全画面背景(羊皮紙調。セーフエリア外まで) | 430×932 | 860×1864 | なし | - | P1 |
| panel_window | 紺+白枠+金縁ウィンドウ(パワー行/針パネル/一覧/結果 共用) | 可変 | 384×384(角96) | なし | ○ | P1 |
| panel_grid_{normal,regen,rainbow,light} | 布グリッド枠+布地(布タイプ4色) | 可変 | 128×128(角24) | 4色 | ○ | P1 |
| cell | 布マス下地(**全状態共通の1種**)。角丸は控えめ(rx8@2x)。**状態色(黄/緑/赤)は数値の文字色で表現し、マスのデザインは統一する**(§4.9。2026-07-09 変更: 旧 cell_{normal,yellow,done,over} の4状態差分と「ティント+インナーリング」表現は廃止)。**角丸と長方形の差分領域は、マス素材の下に布地(布タイプの地色+斜線)のレイヤーを敷いて同デザインで埋める**(布タイプ別に下地を切替。2026-07-08)。**採用素材の角はほぼ直角+角の外側に白系AAが残るため、実装側で rx8@2x(表示4px)の border-radius クリップを適用して角を丸める**(2026-07-10 実機レビュー対応) | 88×58 | 176×116 | なし | - | P1 |
| cell_shitsuke | しつけがけ状態のマス(**マスごと差し替え**。重ね用オーバーレイの画像生成が不調のため 2026-07-09 変更): 白の破線(縫い目)+薄い黄色のティント(2割程度)を焼き込み。右下の「×２」はフォント合成(§4.9) | 88×58 | 176×116 | なし | - | P1 |
| cell_ol_target | 対象プレビュー青枠(重ね)。**注意: 採用素材は外周の角がほぼ直角・不透明**のため、マスに重ねると対象マスの角丸から僅かにはみ出す可能性(W1組込み時に表示確認。2026-07-10) | 88×58 | 176×116 | なし | - | P1 |
| cell_glow | 発光状態のマス(光布。**マスごと差し替え**。2026-07-09 変更): 発光イメージを焼き込み | 88×58 | 176×116 | なし | - | P1 |
| cell_shitsuke_glow | しつけがけ+発光の同時状態のマス(両効果を焼き込んだ合成画像。「×２」はフォント合成。2026-07-09) | 88×58 | 176×116 | なし | - | P1 |
| icon_power | ぬいパワープレート(**全パワー共通の1種**。ベージュの文字なし下地。パワー名はフォント合成し、**パワー別の色は文字色で表現**。§4.9・D2。2026-07-09 変更: 旧5色プレートを廃止) | 58×32 | 116×64 | なし | - | P1 |
| label_power | 「ぬいパワー」タイトルプレート(文字なし下地+フォント合成。D2)。**高さは「ぬいパワー」の文字サイズ準拠で2行分**(12px×行高1.25×2行+上下pad=表示38dp。2026-07-10 変更): 通常は「ぬいパワー」1行、**必殺使用時は「ぬいパワー/会心×２」の2行**(×２は全角)。**文言は上下左右センタリングして中央位置を揃える**(D10) | 68×38 | 136×48 | なし | - | P1 |
| badge_trait_{regen,rain_half,rain_crit,light} | 布特性バッジ(サイクル表示。§5.1.2 案1)。**画像素材を廃止し実装側描画(SVG/CSS)に戻す**(2026-07-10: 画像でも見え方が大きく変わらず、読み込み量節約のため) | 15×15 | - | 4種(半透明は実装側) | - | P1 |
| badge_sparkle | きらめきバッジ(精神統一の残ターン。D5)。**布特性バッジと同サイズに変更**(2026-07-09)。**形状は白発光の円形オーブに変更**(生成物レビューで確定。2026-07-09) | 15×15 | 30×30 | なし | - | P1 |
| btn_header | ヘッダーボタン(戻る/ログ)。**飴色の厚手布張り**(木調から変更。無地のグラデーション+上部ハイライトのみ。2026-07-08)。**色をより濃くして再生成**(2026-07-10 実機レビュー) | 76×40 | 152×80 | 通常/押下 | ○ | P1 |
| btn_skill | 特技ボタン(紺) | 83×44 | 166×88 | 通常/押下/選択中/無効 | ○ | P1 |
| btn_finish | しあげるボタン(赤系) | 83×44 | 166×88 | 通常/押下 | ○ | P1 |
| btn_undo | 1手戻す(紺・小) | 76×44 | 152×88 | 通常/押下 | ○ | P1 |
| btn_primary | 金ボタン(はじめる/もう一度) | 可変×52 | 高104 | 通常/押下/無効 | ○ | P2 |
| btn_secondary | 紺ボタン(リプレイをコピー等) | 可変×46 | 高92 | 通常/押下 | ○ | P2 |
| chip | 丸チップ(部位タブ/フィルター/針/できのよさ) | 可変×28〜36 | 高56〜72 | on/off | ○ | P2 |
| badge_cloth | 布特性バッジ下地(レシピ一覧。文字は重ねる) | 50×20 | 100×40 | 4色 | - | P2 |
| star_result_{on,off} | 結果のできのよさ★ | 40×40 | 80×80 | 2種 | - | P2 |
| fx_hissatsu | 必殺チャージ演出(1枚絵+コードアニメ。D20) | 〜280幅 | 560幅 | なし | - | P3 |
| plate_crit | 会心吹き出しの下地(オレンジ。通常/回復は文字色のみ) | 〜60×24 | 120×48 | なし | - | P3 |
| logo_title | トップ画面タイトルロゴ | 未定 | 未定 | なし | - | P3(トップ画面設計後) |

- 開閉シェブロン(▾)・ソート三角(▼▲)・★チップ文字などは**フォント文字のまま**とする(素材化しない)。
- 結果オーバーレイのスクリム、メッセージ欄・数値行(背景なし)も素材不要。

### 4.5 命名・格納規約(2026-07-08 起票)

- 格納: `assets/ui/<カテゴリ>/` (カテゴリ = bg / panel / btn / cell / icon / badge / fx)
- 命名: `<ID>_<状態>@2x.png`(状態: normal / pressed / selected / disabled / on / off。単一状態は状態部を省略)
- 透過PNG。9-slice の切断位置はパーツごとに一定(角24px@2x を標準)とし、実装形式(W1 の A8 確定後)に合わせてメタデータ化する

### 4.6 生成プロンプト一覧(トーンB基準。§4.4 の全パーツを画像AIで作成: 2026-07-08 決定)

#### 4.6.1 生成の運用ルール

- **共通スタイル(全プロンプトに前置)**: 「和風ファンタジーRPGのゲームUI素材。深い紺色の布地に白の縁取りと金の飾り罫。羊皮紙と織り布・ステッチ(縫い目)の質感。フラットで視認性が高い。単品・正面。**背景はパーツに使われていない単色(例: 純緑 #00FF00)でベタ塗り**(透過や市松模様は指定しない — 市松は描き込まれてしまうため。単色背景はこちらで透過化する)」
- **個別プロンプトで色・縁・装飾を指定した箇所は、共通スタイルの配色(紺・金)より個別指定を優先する**(生成テストで共通スタイルの紺・金装飾が個別指定を上書きする傾向が確認されたため。2026-07-09)
- **文字・ロゴは一切入れない**(日本語文字の生成品質が低かったため例外なしで確定。テキストはすべて実装側でフォント合成する。2026-07-08)
- **px は最終使用サイズ**。同じ縦横比で **2〜4倍の大きさで生成してよい**(縮小・切り出し・透過化はこちらで実施)
- 9-slice 対象(枠・ボタン類)は「**コーナーの装飾は端から辺長の1/6以内に収め、四辺の中間は伸縮しても破綻しない単純な繰り返しにする**」を個別プロンプトに含めている
- 1画像1パーツを推奨(シートにまとめる場合は各パーツの間隔を広めに)

#### 4.6.2 グループ①: 通常パーツ(42点)

| ID | 最終px | 個別プロンプト(共通スタイルに続けて) |
|---|---|---|
| bg_main | 860×1864 | 古い羊皮紙の全画面背景(縦長)。中央がわずかに明るく、四隅に糸と針仕事を思わせるごく薄いステッチ装飾。主張しすぎない |
| panel_window | 512×512 | 装飾ウィンドウ枠。深い紺の面+白の外縁+金の飾り罫。コーナー装飾は端から1/6以内、四辺の中間は単純な繰り返し。正方形 |
| panel_grid_normal | 512×512 | 布グリッドの外枠。内側はベージュの布地テクスチャ(斜め織り)で面の大部分を占める。枠は細い金+白の縁のみ。紺色やその他の色の太い帯・布地の枠は入れない。コーナー装飾は端から辺長の1/6以内に小さく控えめに |
| panel_grid_regen | 512×512 | 同上、内側は深緑の布地テクスチャ |
| panel_grid_rainbow | 512×512 | 同上、内側は芥子色(落ち着いた黄)の布地テクスチャ |
| panel_grid_light | 512×512 | 同上、内側は落ち着いた桃色の布地テクスチャ |
| cell | 176×116 | 布のマス(横長タイル・小さな角丸)。クリーム色の織り地、上辺にわずかなハイライト。縁は細い焦茶の1本線のみ。紺色・金色・コーナー装飾・白の外縁は入れない。装飾なしの無地 |
| cell_shitsuke | 176×116 | 同上のクリーム地のマス全面に、**はっきり分かる黄色のティント(4割程度)**を重ね(2026-07-10 実機レビューで黄味不足のため 2割→4割 に強調)、**しつけ縫いを表す白の破線(ミシン目状の縫い目)をマス面を横切る形で数本はっきり描く**(2026-07-09 破線が出なかったため強調)。文字は入れない(「×２」はフォント合成) |
| cell_ol_target | 176×116 | 重ねる「枠のみ」の素材。濃紺の太枠(2026-07-09 生成物採用に合わせ「鮮やかな青」から変更)。中央は背景色のまま空ける |
| cell_glow | 176×116 | cell と同じクリーム地のマスに、**桃色の柔らかな発光がマスの全面を覆った状態**を焼き込んだもの。**中央に寄せず、四隅まで発光で満たす**(2026-07-10 実機レビューで発光が中央寄りだったため全面化) |
| cell_shitsuke_glow | 176×116 | cell_shitsuke と同じ黄ティント+白破線のマスに、cell_glow と同じ桃色の発光を全面に重ねて焼き込んだもの。**ティント・発光ともひと目で「しつけ+発光」と分かる濃さにする**(2026-07-10 実機レビューで色が薄かったため強調) |
| badge_trait_regen | 30×30 | **(4種とも廃止 2026-07-10: 実装側描画(SVG/CSS)に戻したため生成不要。§4.4)** ~~小さな円形バッジ。深緑の地に白縁、左上に小さな光沢~~ |
| badge_trait_rain_half | 30×30 | ~~同上、水色の地~~(廃止) |
| badge_trait_rain_crit | 30×30 | ~~同上、黄(芥子色)の地~~(廃止) |
| badge_trait_light | 30×30 | ~~同上、桃色の地~~(廃止) |
| badge_sparkle | 30×30 | 白く柔らかに発光する円形のオーブ。白のリング、左上に小さな光沢(2026-07-09 生成物採用に合わせ「縦長楕円+十字光条」から変更) |
| btn_header_normal | 152×80 | 厚手の布張りの小ボタン(横長・角丸)。**濃い飴色(こげ茶寄り)の布地**(2026-07-10 実機レビューで色をより濃く)、暗色の縁と縁沿いのステッチ(縫い目)、上辺ハイライト。無地で模様は入れない |
| btn_header_pressed | 152×80 | 同上を全体的にひと段暗く沈めた押下状態 |
| btn_skill_normal | 166×88 | 紺地の小ボタン(横長・角丸)。白の細縁+内側に金のキーライン、上辺に淡い光沢。コーナー装飾は端から1/5以内 |
| btn_skill_pressed | 166×88 | 同上をひと段暗く沈めた押下状態 |
| btn_skill_selected | 166×88 | 同型で金地(真鍮)のボタン。白の細縁 |
| btn_skill_disabled | 166×88 | 同型で彩度を落とした灰色のボタン |
| btn_finish_normal | 166×88 | 同型で深い臙脂(暗赤)のボタン。白の細縁+金のキーライン |
| btn_finish_pressed | 166×88 | 同上をひと段暗く沈めた押下状態 |
| btn_undo_normal | 152×88 | 同型で紺地+金の縁のボタン |
| btn_undo_pressed | 152×88 | 同上をひと段暗く沈めた押下状態 |
| btn_primary_normal | 480×104 | 金地の大きめボタン(横長・角丸)。白縁+豪華な光沢。コーナー装飾は端から1/6以内 |
| btn_primary_pressed | 480×104 | 同上をひと段暗く沈めた押下状態 |
| btn_primary_disabled | 480×104 | 同型で彩度を落とした灰色 |
| btn_secondary_normal | 400×92 | 紺地の大きめボタン(横長・角丸)。白の細縁 |
| btn_secondary_pressed | 400×92 | 同上をひと段暗く沈めた押下状態 |
| chip_on | 144×64 | 小さなカプセル形チップ。金地に白の細縁、上辺に光沢 |
| chip_off | 144×64 | 小さなカプセル形チップ。紺地に薄い白縁 |
| badge_cloth_regen | 100×40 | 横長カプセル形のラベル下地。深緑の無地に白の細縁 |
| badge_cloth_rainbow | 100×40 | 同上、芥子色(黄)の地 |
| badge_cloth_light | 100×40 | 同上、桃色の地 |
| badge_cloth_normal | 100×40 | 同上、ベージュの地 |
| star_result_on | 80×80 | 大きな金色の星。輝きの光沢と柔らかな発光 |
| star_result_off | 80×80 | 同型の星の未点灯表現。灰色に沈み、くぼんだ質感 |
| plate_crit | 120×48 | 小さな横長の吹き出し下地(角丸)。鮮やかな橙の無地に白の細縁 |
| icon_power | 116×64 | 横長の小プレート(角丸)。背景・布マスと同系統のベージュの無地にベベル、上辺に淡い光沢。文字は入れない(パワー名はフォント合成) |
| label_power | 136×48 | 金のプレート(角丸)。ベベルと上辺の光沢。文字は入れない(「ぬいパワー」はフォント合成) |

#### 4.6.3 グループ②: アニメーション系(1点。**生成結果をテストしてから実装採否を判断**)

文字入りパーツは生成テストで日本語文字の品質が低かったため**不採用が確定**(2026-07-08)。ぬいパワー類は文字なしプレート+フォント合成としてグループ①へ移動した。残るは演出系のみ: コマ割りの扱いが読めないため生成結果を確認し、品質が出なければ従来方式(1枚絵+コードアニメーション)に切り替える。

| ID | 最終px | 個別プロンプト(共通スタイルに続けて) |
|---|---|---|
| fx_hissatsu | 560×320 | 必殺チャージの演出画。橙の光が爆ぜる帯状のフラッシュと気の奔流。中央にスペースを残す(文言はコード側で重ねる)。可能なら3〜6コマの連番でも可(拡大・フェードはコード側で実施) |

- logo_title(トップ画面タイトルロゴ)はトップ画面の設計後に検討(P3)。文字を画像に含めない方針のため、装飾画+フォント合成を基本とする。

### 4.7 素材の制作分担(改定: 2026-07-08)

- **§4.4 の全パーツを画像AI生成(ユーザー担当)に統一する**(2026-07-08 改定)。当初は「枠・ボタン類=SVG内製/絵画的パーツ=画像AI」の分担としたが、第1弾の組込みで**画像部分とSVG部分の質感差が顕著**になったため、全面的に画像AI素材へ置き換える
- プロンプトは §4.6 の一覧を使用。切り出し・透過化・リサイズ・9-slice 化・モック組込みは Claude 担当
- 既存のSVG素材(27点)は**生成素材が揃うまでのプレースホルダ**として残す
- 文字入りは生成テストの結果、**不採用が確定**(日本語文字の品質不足。文字なしプレート+フォント合成に切替。2026-07-08)。演出系(fx_hissatsu)のみ生成テスト後に採否を判断し、不採用なら1枚絵+コードアニメーションに戻す

### 4.8 画像AI生成素材の組込み(2026-07-09 全面組込み。第1弾 2026-07-08 の art_* とSVGプレースホルダは全て置換・削除)

§4.4 の全パーツ(fx_hissatsu を除く42点)をユーザー生成の画像AI素材に置き換え、モック v2.4 に組込み済み。

- **原本**: `assets-src/<ID>.png`(不採用・旧版は `assets-src/ng/`。git 管理外)。**加工スクリプト**: `assets-src/process-assets.py`(透過化=緑背景のクロマアンミックス、トリム=✦ウォーターマーク除外の被覆率判定、リサイズ)、`assets-src/make-font-subset.py`(フォントサブセット)
- **出力**: `public/mock/assets/<ID>.png`(§4.4 の最終px。**9-slice 対象は生成物の縦横比を保持して高さ基準で出力**(幅可変)— border-image で伸縮するため装飾が歪まない)
- **スライス値(@2x)**: panel_window=96(表示: 小型パネル10px/結果窓28px)、panel_grid=24(表示12px)、btn_skill/finish/undo=26(表示8px)、btn_primary=50・btn_secondary=45(表示 縦20px・横14px)。チップ・マス・バッジ類は背景表示(100%伸縮)
- **透過化の方式**: 境界・グロー帯は緑過剰(G−max(R,B))によるα推定+アンミックス。布地内部は3px侵食マスクでα=1保護(緑系布地の変色防止)。広い発光を持つ star_result_on/off・badge_sparkle は全域クロマα
- **緑相ワイプ**(2026-07-10 追加): 正当な緑を含まない12点(cell系5種・btn_skill系4種・btn_finish系2種・icon_power)は、緑背景のビネット・焼き込み影の明度ムラが「背景色からの距離」判定をすり抜けて半透明ヘイズ/緑残りになるため、**色味比率 greenness=(G−max(R,B))/max(R,G,B)** で判定し、greenness>0.35 は侵食保護より優先して α=0 に確定、中間帯(0.12〜0.35)はアンチエイリアスとして半透明化する。角丸の外側は完全透過になる(実機レビュー指摘の対応)
- **白リム剥離**(2026-07-10 追加): btn_skill系4種・btn_finish系2種は生成時に**全周へ純白アウトライン(最外周・四隅では弧沿いに幅広)が焼き込まれて**おり、角の外側は透過済みでも弧上の白がα≈1で残るため「角が透過されていない」ように見える(実機レビュー指摘)。透明領域に接する「ほぼ無彩色の白」(unmix後RGBで min≥180 かつ max−min≤50)を外周から連鎖的に剥離してα=0にする。内側の装飾(金=黄寄り・紺=青寄り)は彩度で連鎖が止まる。btn_skill_pressed のみリムが暗いグレーに落ちているため中明度の完全無彩色グレー(min≥90 かつ max−min≤20)も対象(※disabled はグレーアウト本体が無彩色のため白判定のみ)。btn_undo/btn_header は最外周が金色/暗色のため対象外。**仕上げとしてリサイズ後の最終出力解像度でもハロー除去を行う**(2026-07-10 追加): ソース段階の剥離は列ごとに停止位置が揺れ、LANCZOS縮小が取り残しを外周へ半透明の白/明灰として滲ませる。@2x表示では9-sliceコーナーがほぼ等倍で描画されるため紺縁の外の明るい画素は1個でも視認される(「btn_undo と同水準なら許容」の当初判定は誤り — btn_undo は明るい金縁の外の白で目立たないが、こちらは**暗い紺縁の外の白**でコントラストが立つ)。透明領域(画像外含む)に接する明るい無彩色画素(min≥150 かつ max−min≤80)を最終画素で連鎖除去。選択(金)ボタンのみ縁自体が明るいため純白寄り(min≥200 かつ max−min≤45)に限定し、クリーム〜淡金の縁は残す
- fx_hissatsu は黒背景6コマ連番のまま原本保管(演出実装時に加算合成・コマ分割)

#### 4.8.1 実機レビュー指摘と残修正タスク(2026-07-10 起票)

モック v2.4 の実機レビュー(2026-07-09〜10)での指摘。設計判断は v0.44 で反映。**R1〜R8 は対応済み**(モック v2.5。sonnet 委譲・2026-07-10)。**R9 も対応済み**(再生成5点=cell_shitsuke / cell_glow / cell_shitsuke_glow / btn_header_normal / btn_header_pressed を採用・再加工・組込み。2026-07-10)— **全タスク完了**。

| # | タスク | 内容 |
|---|---|---|
| R1 | 余白を画面下端へ | S2 変更の反映。モック `main` の `justify-content: center` をやめ上詰めにし、余りの縦余白を画面下端に集める(ヘッダーとぬいパワー行の間に隙間を作らない) |
| R2 | 布特性バッジのSVG戻し | `badge_trait_*.png` を撤去し、実装側描画(CSS/SVG の色付き円形バッジ。旧モックの方式)に戻す(§4.4) |
| R3 | きらめきの表示制御と正円化 | サイクル全体表示では非表示にする(D5)。加工済み badge_sparkle.png が横長の楕円になっている(被覆率トリムが原因)ため、**正方形の中心クロップ**で再加工して正円に戻す |
| R4 | グリッド内部の旧CSS布地の置換 | 「SVGが残って見える」指摘の正体は、マス間の隙間・グリッド内余白を埋めている**旧CSSグラデーションの布地表現**(緑+斜線)。panel_grid_* 素材は中面が布地で埋まっているため、`.grid` の border-image に **`fill`** を指定して素材の中面で埋め、CSSグラデ層(`.grid` と `.cell` 系の下敷き)を撤去する |
| R5 | できのよさ★の枠数修正 | モックの★が4枠(点灯3+消灯1)になっている誤りを**3枠**に修正。合わせて star_result_on/off のトリム修正: 被覆率しきい値15%が星の細い先端を切り落とし形が崩れているため、✦ウォーターマーク領域(右下)を除外した低しきい値トリムに変更して再加工 |
| R6 | 結果ボタンの文字縦位置 | 「リプレイをコピー」「もう一度」の文字がボタンの縦中央にない。pill 型 border-image(上下 border 20px)の内容領域が薄いことに起因するため、line-height / padding / transform で微調整し 360dp 幅で確認 |
| R7 | btn_skill_selected の緑残り | 選択中特技ボタンの外周に半透明の緑が残る(白リム外周の**落ち影が緑背景と混色**したまま残存)。加工スクリプトに低α画素の緑被り抑制(例: `G ≤ max(R,B)` クランプ)を追加して再加工 |
| R8 | ヘッダー「◀もどる」 | プレイ画面の「◀ 戻る」を「◀もどる」に変更し、「◀」は本文より小さいフォントサイズにする(D39) |
| R9 | 再生成素材の差し替え | ユーザー再生成待ち: cell_shitsuke(黄4割)/ cell_glow(全面発光)/ cell_shitsuke_glow(濃く)/ btn_header 2種(濃い飴色)。格納後に `assets-src/process-assets.py` で再加工して組込み |

### 4.9 フォント・文字合成スタイル(2026-07-09 決定)

- **UIフォント: 源暎ラテミン v2**(`GenEiLateMin_v2.ttc`。本端末インストール済み)。アプリ・モックには **woff2 サブセットを埋め込んで**使用する(端末フォントに依存しない。S6「OSフォントスケールに追従しない」とも整合)。フリーフォントだが、同梱時はライセンス文を添付する(実装時に確認)。
- **標準文字スタイル: 黒縁白抜き+ドロップシャドウ**。実装は2レイヤー方式(縁取りレイヤー=`-webkit-text-stroke`+塗りレイヤー=`background-clip: text`)。参考実装: `assets-src/text-style-reference.html`。
- **文字色を白以外にする場合は縦グラデーション(上=明・下=濃)をかける**。
- 適用箇所:
  - **布グリッドの残り数値**: 基本は白。**マス状態色(D14)は数値の文字色(白抜き部分)で表現する**: 黄ゲージ内=黄 / 縫い終わり=緑 / 縫いすぎ=赤(各グラデ)。**縁の色も文字色に合わせる**: 白数字=黒縁、黄数字=濃い黄、緑数字=濃い緑、赤数字=濃い赤(2026-07-09)。マス下地は全状態共通(cell 1種)。
  - **しつけがけマスの「×２」(全角)**: 黄(グラデ)、**残り数値の6割程度のサイズ**で右下に表示。**縁取りは付けない**(グラデ塗り+ドロップシャドウのみ。2026-07-09)。
  - **ぬいパワー名**: 共通ベージュプレート(icon_power)に重ね、**パワー別の色は文字色で表現**: 弱い=青 / 普通=緑 / 強い=橙 / 最強=赤 / ？=灰(各グラデ。旧5色プレートの色を継承 — モックで見た目を確認して調整)。**「？」は全角**とする。
  - 「ぬいパワー」タイトル(label_power)ほか、プレート・ボタン上の合成文字も同スタイルを基本とする。

## 5. 表示項目インベントリ(ゼロベースの要不要確認)

ブラウザ版の全表示項目を列挙し、スマホ版での要否を決める。**「判定」列が確定するまで未決**。判定の区分: ◎=常時表示 / ○=表示するが常時でなくてよい(タップで開く・別画面等) / △=検証モード等の限定表示 / ×=スマホ版では不要。

「推奨」列は判断のたたき台(未承認)。**確定した行が計算機モード検討(別セッション)への入力になる。**

### 5.1 プレイ中の状態表示(判定確定: 2026-07-08)

| # | 項目 | 判定 | 決定内容 |
|---|---|---|---|
| D1 | ターン数 | ◎ | **布グリッドの下**に表示領域を設ける。D6(集中力)と1行に収めるのが理想 |
| D2 | 現在ぬいパワー | ◎ | **布グリッドの上**に表示領域を設ける。プレートは**全パワー共通のベージュ下地1種**(icon_power)とし、パワー名は黒縁白抜き+ドロップシャドウのフォント合成、**パワー別の色は文字色で表現**(§4.9。2026-07-09 変更: 当初の「それぞれデザインした画像」→5色プレート案を廃止)。「？」は全角 |
| D3 | 次パワー予告 | ◎ | 予告表示は**あり固定**(ON/OFF設定は持たない)。「→」の表示は廃止し、**ぬいパワーアイコンを並べる**形にする。**予告は3つ先まで(現在と合わせて4つ)表示**(2026-07-08 追加)。**アイコンの大きさは現在と次以降で差をつけない**(同一サイズ。2026-07-08)。**特技使用時はモーションを付ける: 現在のぬいパワーが消去され、次のぬいパワーがスライドして現在位置に入る**(2026-07-08) |
| D4 | サイクル全体表示(下線予告つき) | ○ | 常時表示せず**画面切り替えで表示**。切り替えボタンは D2/D3 の並びに領域があればそこへ。表示方式は検討中(§5.1.1) |
| D5 | 精神統一固定中(残ターン) | ◎ | **残り固定ターン数と同数のバッジをぬいパワーアイコンに付ける**。バッジは**きらめきをイメージした画像**とする(2026-07-08 変更。当初の「淡い青色の発光」案は廃止)。**サイズは布特性バッジと同じ15×15dp**、**形状は白発光の円形オーブ**(生成物レビューで縦長楕円から変更。2026-07-09)。**表示はコンパクト表示のみとし、サイクル全体表示では表示しない**(布特性バッジ(プレート右上)と3個表示時に重なるため。2026-07-10)。**複数並ぶ場合は左端の表示を基準に、中心間距離12pxで並べる**(素材は発光の裾を含み画像端=見た目の境界ではないため、グロー同士の重なり(画像3px分)を許容して詰める。2026-07-10 「間隔1px」から変更) |
| D6 | 集中力 数値 | ◎ | **布グリッドの下**に表示領域。ターン数(D1)と1行に収めるのが理想 |
| D7 | 集中力バー | × | 廃止 |
| D8 | 集中力内訳(基礎+針+開幕) | × | プレイ画面から削除し、**レシピ選択画面(§5.7)へ移動**。具体的な表示位置はモック作成後に議論 |
| D9 | 布特性(タイプ+次回発動ターン+内容) | × | 廃止(発動タイミングの情報はサイクル表示(D4)の下線予告と、実発動時の演出で把握する) |
| D10 | 必殺チャージ中バッジ | × | 常時の状態表示バッジは廃止(2026-07-08 判定訂正)。チャージ発生時の演出は D20 で表現する。**保持中の状態は「無我の境地」ボタンの有効/無効で表す**(必殺チャージの効果は「無我の境地」が実行可能になることのみであるため、チャージ保持中のみ同ボタンを有効にすれば足りる。2026-07-08)。**必殺使用時(効果中)は、ぬいパワータイトルプレート(label_power)の文言を「ぬいパワー/会心×２」の2行表示に変更する**(×２全角・センタリング。プレートは常時2行分の高さ。2026-07-10) |
| D11 | 無我の境地バッジ | × | バッジは廃止。特技欄「無我の境地」ボタンの**有効化(発動中)表現で代替**する |

#### 5.1.1 サイクル全体表示の切り替え方式(決定: 2026-07-08)

**案A「パワー表示行のその場トグル」を採用** — D2/D3 のパワー表示行全体をタップ可能にし、タップで「現在+次3つのアイコン」⇔「サイクル全体(現在位置ハイライト+布特性予告つき)」を同じ領域で切り替える。行の右端に小さな開閉アイコン(シェブロン等)を置いてタップできることを示す。別レイヤーを持たず高さも固定できるため部品が最少。

- **サイクル全体表示は最大10個になるため、左右スライドで横スクロールできるようにする**(2026-07-08)。
- パワーアイコンへの実行ターン番号(T番号)の併記は**行わない**(2026-07-08)。
- 行の高さは控えめでよい(第1版モックより縮小)。
- 表示が小さくなりすぎる等の問題が生じた場合は再検討する(代替: 案B=行直下へのポップオーバー表示)。

#### 5.1.2 サイクル表示での布特性予告の表現(決定: 2026-07-09)

ブラウザ版の**下線**(実線/点線・色分け。SPEC §4.3)はブラウザ版の実装優先で決めた仕様であり、**スマホ版では変更する**(2026-07-08 決定)。D9 廃止により布特性の事前情報はこの表示に集約される。

**案1「特性アイコンバッジ」で確定**(2026-07-09):

- サイクル表示時、対象エントリの**ぬいパワープレートの右上**に**布特性バッジ**(再生=緑 / 虹・半減=水色 / 虹・会心=黄 / 光=ピンク。15×15dp)を重ねる。次の周回で到達する分(ブラウザ版の点線相当)は**半透明バッジ**で表す。実行ターン番号の併記はしない(2026-07-08)。
- **再生バッジは4の倍数ターン(4, 8, 12, …)のエントリに表示する**(2026-07-09 仕様変更)。SPEC §3.6 の発動タイミング(5, 9, 13, …)自体は変更しない。虹・光のバッジは従来どおり発動ターン(5, 9, 13, …)のエントリに表示する。
- (不採用)案2: アイコンの色枠/グロー — D5 のきらめき表現と混同しやすいため見送り。

### 5.2 布グリッド(判定確定: 2026-07-08。記載のない項目は推奨案を採用)

| # | 項目 | ブラウザ版 | 推奨 | 判定 |
|---|---|---|---|---|
| D12 | マスの残り数値 | 常時 | ◎ | ◎ |
| D13 | マスの基準値(/100 等) | 常時 | ○(タップ詳細 or 小さく) | **○ 確定 (2026-07-08)**: 表示有無を**ユーザー設定化**(新規設定項目)。表示時はブラウザ版同様に小さく併記、非表示時は残り数値の文字サイズを上げる。**デフォルトは非表示** |
| D14 | マス状態色(白/黄/緑/赤) | 常時 | ◎ | ◎(表現変更 2026-07-09: マス下地は共通1種とし、**状態色は残り数値の文字色で表す**。§4.9) |
| D15 | しつけがけ枠 | 発生時 | ◎ | ◎(表現変更 2026-07-09: 重ね枠でなく**マスごと差し替え**(cell_shitsuke)。右下に「×２」(全角・黄)を数値の6割サイズでフォント合成。§4.9) |
| D16 | 発光マス(光布) | 発生時 | ◎ | ◎(表現変更 2026-07-09: **マスごと差し替え**(cell_glow)。しつけがけと同時発生時は合成画像 cell_shitsuke_glow を使用) |
| D17 | 対象範囲プレビュー(青枠) | 特技選択時 | ◎(操作系の踏襲対象) | ◎ |
| D18 | 布タイプ背景色 | 常時 | ○(D9 とどちらかでも成立) | ○(D9・D24 廃止に伴い、プレイ画面で布タイプを示す唯一の表示となる) |
| D19 | ダメージ/会心/回復の吹き出し | 一時表示 | ◎ | ◎ |
| D20 | 必殺チャージ演出 | 一時表示 | ◎ | **◎ 確定 (2026-07-08)**: **画像+エフェクト**で用意する(常時バッジ D10 は廃止し、こちらに集約) |
| D21 | 誤差評価値(合計) | 常時 | ◎(SPEC: 常時表示) | **◎ 確定 (2026-07-08)**: グリッド下の**ターン数・集中力と同じ行**に表示。表示文言は「**誤差**」 |
| D22 | ★3ライン(≤N) | 常時 | ◎ | **× 確定 (2026-07-08)**: 表示不要 |
| D23 | 凡例(色の意味) | 常時 | ×(ヘルプ/初回チュートリアルへ) | **× 確定 (2026-07-08)**: プレイ画面からは削除し、**ヘルプ(§5.8)を新設してそちらへ移動** |
| D24 | 布タイプ名の行 | 常時 | ×(D9 へ統合) | ×(廃止。布タイプは D18 の背景色とレシピ選択画面で示す) |

### 5.3 特技・操作(判定確定: 2026-07-08。記載のない項目は推奨案を採用)

| # | 項目 | ブラウザ版 | 推奨 | 判定 |
|---|---|---|---|---|
| D25 | 特技ボタン(20個)+消費集中力 | 常時 | ◎(配置・グルーピングは画面設計で) | ◎: **5行4列**。**配置順はユーザー設定化**(新規設定 C2)。デフォルト配置は §5.3.1(レベル習得順はなじみがないため採用しない) |
| D26 | 特技の選択中表示 | 選択時 | ◎ | ◎ |
| D27 | 特技の使用不可表示(集中力不足) | 常時 | ◎ | ◎ |
| D28 | しあげるボタン | 常時 | ◎(誤タップ対策を画面設計で) | ◎: 専用の誤タップ対策は設けない(誤操作は「1手戻す」で復帰できるため) |

#### 5.3.1 特技ボタンのデフォルト配置(5行4列)

ユーザー指定順(1列目の上から下、次いで2列目…の**列優先**)を行×列に展開したもの。列ごとに系統がまとまる(1列目=ライン系 / 2列目=特殊ぬい / 3列目=倍率・補助 / 4列目=ユーティリティ+基本)。

| 行\列 | 1列目 | 2列目 | 3列目 | 4列目 |
|---|---|---|---|---|
| 1 | ヨコぬい | 逆たすきぬい | 2倍ぬい | ぬいパワーシフト |
| 2 | 水平ぬい | 巻きこみぬい | 3倍ぬい | しつけがけ |
| 3 | 滝のぼり | みだれぬい | ねらいぬい | 無我の境地 |
| 4 | 大滝のぼり | かげんぬい | 精神統一 | ぬう |
| 5 | たすきぬい | 半かげんぬい | 糸ほぐし | しあげる |

(指定時の「たすきがけ/逆たすきがけ/巻き込みぬい」は SPEC の正式名「たすきぬい/逆たすきぬい/巻きこみぬい」として記録)

- ボタン幅を4列均等に保つため、**「ぬいパワーシフト」のみ特技名を「ぬいパワー」「シフト」の2行に分けた3行レイアウト**(特技名2行+消費集中力)とする(2026-07-08)。

### 5.4 行動ログ(判定確定: 2026-07-08)

| # | 項目 | ブラウザ版 | 推奨 | 判定 |
|---|---|---|---|---|
| D29 | 行動ログ(全文) | 常時(下部) | ○(別パネル/引き出し表示) | ○: 常時表示は不要。ヘルプ同様、**必要時に呼び出す別画面**とする |
| D30 | 現在ターンのハイライト | 常時 | △(検証モードとセット) | △: **リプレイモード(§5.4.1)のログ表示で使用** |
| D31 | 出目・会心率表示 | 検証モード | △ | **出目は表示する。会心率表示はスマホ版では削除** |

#### 5.4.1 リプレイモード(方針決定: 2026-07-08、方式は検討中)

**スマホ版では検証モードの選択(トグル)は設けない**。代わりに**リプレイモード**を用意する:

- リプレイモード中は、**特技欄の領域とトグルで行動ログを表示**する方式を検討する(通常プレイのログ呼び出し(D29)とは別に、リプレイ追跡用の表示)。
- 現在ターンのハイライト(D30)はこのログ表示で使用する。
- **入り口**: シミュレーターの起動(レシピ選択画面)とは別に、**リプレイモードの入り口**を用意し、リプレイ読込はそこで行う(D38)。
- **操作**: 1手進むはリプレイモード限定。1手戻すは通常プレイでも常設(D37)。
- シードはスマホ版では表示・指定とも設けない(D36。再現はリプレイデータ経由)。
- 結果画面の誤差内訳表(D41)の扱いは §5.6 の判定で確定する。

### 5.5 設定・セッション操作(判定確定: 2026-07-08)

| # | 項目 | ブラウザ版 | 推奨 | 判定 |
|---|---|---|---|---|
| D32 | レシピ選択 | ヘッダー常時 | ○(開始前画面/メニューへ。プレイ中は非表示) | ○: **開始前の選択画面(§5.7)へ移動** |
| D33 | 針選択+★ | ヘッダー常時 | ○(同上) | ○: **開始前の選択画面(§5.7)へ移動** |
| D34 | サイクル予告表示 ON/OFF | ヘッダー常時 | ○(設定画面へ) | ×: 削除(予告あり固定。D3) |
| D35 | 検証モードトグル | ヘッダー常時 | ○(設定画面へ) | ×: 削除(検証モード自体を廃止。§5.4.1) |
| D36 | シード表示・指定 | 検証モード | △ | ×: スマホ版では表示しない |
| D37 | 1手戻す / 1手進む | 検証モード | △ | **1手戻す=◎ 常時**(通常プレイでも常設。D28 の誤タップ復帰手段)/ **1手進む=△ リプレイモードのみ** |
| D38 | リプレイコピー / 読込 | 検証モード等 | △ | コピー=○ 用意する。**コピー内容は SNS 等で共有できるよう圧縮フォーマットを別途設計**。読込=**シミュレーター起動とは別にリプレイモードの入り口を用意し、そちらで実施** |
| D39 | 新しく始める | ヘッダー常時 | ◎(プレイ中も要る。誤タップ対策要) | ◎: プレイ画面に**ヘッダーを新設**し、**左側に「◀もどる」ボタン**(開始前の選択画面へ戻る=新規開始の導線)、**中央に選択中のレシピ名**を表示する。文言は**ひらがな「もどる」でレシピ選択画面と統一**し、**「◀」は本文より小さいフォントサイズ**にする(2026-07-10) |

### 5.6 結果画面(判定確定: 2026-07-08)

**結果画面はプレイ画面上のオーバーレイ表示**とする(独立画面にしない)。

モック第1版の構成(2026-07-08 作成、レビュー待ち):

1. 画面全体を暗転(半透明スクリム。背後にプレイ画面が透ける)し、中央にウィンドウを表示
2. ウィンドウ内(上から): 「できのよさ」ラベル → **★を大きく表示**(D40) → 使用ターン数・残り集中力(D42。ラベル左・数値右寄せ) → **リプレイをコピー**(D43。押下で「コピーしました」1.5秒表示)+**もう一度**(D44。同条件で新規ゲーム)の2ボタン → 小さく「↩1手戻す(盤面を確認する)」
3. 「1手戻す」はしあげる直前の状態に戻る=オーバーレイが閉じ、最終盤面を確認できる(D37 の常設 1手戻す を結果画面にも適用する提案)

| # | 項目 | ブラウザ版 | 推奨 | 判定 |
|---|---|---|---|---|
| D40 | ★数・できのよさ | 表示 | ◎ | ◎ |
| D41 | 誤差内訳表(マス別) | 検証モード | △ | ×: 不要 |
| D42 | 使用ターン数・残集中力 | 表示 | ◎ | ◎ |
| D43 | リプレイコピー | 表示 | ○ | ○: 用意する(コピー内容は圧縮フォーマット。D38) |
| D44 | もう一度(新しく始める) | 表示 | ◎ | ◎: **レシピ選択・針選択はそのままで、新しくもう1ゲームを開始する**動作とする(選択画面へは戻らない) |

### 5.6.5 追加の表示要素(2026-07-08 決定)

| # | 項目 | 決定内容 |
|---|---|---|
| D45 | **メッセージ欄** | 数値・メッセージ領域は**2行×4列のグリッド**で構成する(2026-07-08 確定): 1行目=ターン(1列目)/集中力(2列目)/誤差(3列目)、2行目の1〜3列=メッセージ欄、4列目の1〜2行=1手戻すボタン。**1行目と2行目の間に1〜3列目の幅で罫線**を引いて区別する。メッセージ欄の背景はウィンドウ調(紺+白枠)にしない(ボタンと混同するため)。開幕効果・布特性の発生時に以下のメッセージを表示する(**「!」は全角を使用**) |
| D46 | **広告表示領域** | **画面最上部**に確保する。**アンカーバナー(アンカーアダプティブ)を想定**(2026-07-08): 幅は**フレーム幅いっぱい**(余白・角丸なし。タブレットはフレーム上限に合わせた幅を要求)、高さは**SDK取得値で可変(概ね50〜90dp、下限50)**とし、起動時に取得した高さで領域を確保して差分は上下の伸縮余白(S2)で吸収する。上部配置の理由: 最頻タップ領域(特技ボタン=下部)から離し誤タップを防ぐ(広告の誤クリック誘発はポリシーリスク)。実高さの仕様は実装時(W1)にSDKで確認 |
| D47 | ヘッダーの整形 | ログ呼び出しボタンは**戻るボタンとサイズを揃え**、レシピ名が**中央**に配置されるようにする。レシピ名のフォントは**最大9文字が下限幅(360dp)で収まる上限サイズ**とする(左右の余白を詰めてモック実測20px。2026-07-08) |
| D48 | 布グリッドの高さ | 大きさに余裕があるため、必要に応じて**縦方向を縮めてよい**(横幅は維持) |
| D49 | メッセージの配置 | メッセージは**メッセージ欄内で中央寄せ**(2026-07-08) |
| D50 | 数値・メッセージ領域の表記詳細 | 集中力のラベルは**「残り」+改行+「集中力」の2行表記**。**1行目(数値行)は高さを広めに、2行目(メッセージ欄)は低めに**する(2026-07-08)。**「ターン」「残り集中力」「誤差」のラベルと3つの数値は下端を揃える。「残り」の左端は「集中力」の左端に揃える**(左寄せ。2026-07-08)。数値のフォントはラベルより大きくし、**誤差が4桁になっても下限幅(360dp)で崩れないサイズを上限**とする。1行目の数値は**22px**。**各数値に最大桁数分の幅を予約**(ターン=2桁 / 集中力=3桁 / 誤差=4桁)し、**ラベル位置は桁数によらず固定**する。4項目(3項目+1手戻す)は**行幅に均等配分**(左詰めにしない)。**1手戻すボタンの幅・位置は特技ボタンの4列目に揃える**(2026-07-08)。モック実測: 間隔17px(375)/10px(360)、下限360dp・誤差9999でもはみ出しなし(2026-07-08)。**文字表示領域一式(数値3項目+メッセージ欄)は「ターン」ラベルのフォントサイズ基準で0.5文字(13px×0.5≒7px)右に寄せる。1手戻すボタン(4列目)は動かさない**(2026-07-10) |
| D51 | **管理者モード時の広告非表示** | 管理者モードでは**広告(D46)を表示しない**。空いた分は**布グリッドを拡大**して使う。管理者モードの仕様自体は**別途決定**(シード入力の開放(§3 S9)もその候補) |

D45 のメッセージ文言(実機準拠。再生布の特性発動は**表示なし**):

| 事象 | メッセージ |
|---|---|
| 開幕集中力アップ(奇跡の針) | 奇跡の効果で 集中力が 30アップ! |
| 開幕必殺チャージ(光の針) | 光の効果で ひっさつチャージ!! |
| 虹布特性(集中力半減) | 消費集中力が 半分になった! |
| 虹布特性(会心率アップ) | 消費集中力が1.5倍で 会心率アップ! |
| 光布特性 | 1つのマスの 威力と会心率がアップ! |
| 再生布特性 | (表示なし) |

### 5.7 レシピ選択画面(新設合意: 2026-07-08)

プレイ画面とは別に**レシピ選択画面**を設けることを合意。プレイ画面から外す開始前情報の移動先とする。収載: レシピ選択(D32)・針選択+★(D33)・開始集中力の内訳(D8)。

画面構成(モック第2版。2026-07-08 レビュー反映):

1. 広告領域(D46)+ヘッダー(左「◀もどる」=トップへ / 中央タイトル「レシピをえらぶ」)
2. **部位タブ**(**頭/体上/体下/腕/足/ラグ/ぬいぐるみ** の順。横スクロール)
3. **フィルター**(デフォルトはたたまれており、タップで開閉。項目: **装備可能レベル / 布特性**。必要職人レベルは使用シーンが少ないため対象外(2026-07-08 決定)。装備可能レベルの選択肢は**「すべて」+レシピに実在するLvの降順**を横スクロールで並べる。**選択肢はラベルと同一行に配置**(改行しない)し、**各行の「すべて」の開始横位置を揃える**)+**ソート**(装備可能レベルの**降順/昇順のみ**。記号は**小型の三角「▼/▲」**(本体文字より一回り小さく表示))
4. **レシピ一覧**(選択中の部位のレシピ。行=レシピ名+装備Lv+布タイプバッジ(再生=緑/虹=黄/光=ピンク/通常=ベージュ)。**バッジは文字数によらず固定幅+中央揃え**(2026-07-08)。**この画面では一覧が高さの伸縮を吸収**(S2)し、縦スクロール)
5. **さいほう針**(表示文言「さいほう針」。並び順は**光→奇跡→超→プラチナ→銀→鉄→銅**。横スクロールのチップ)+**できのよさ**(表示文言「できのよさ」。★0〜★3)。**針とできのよさはローカルに最終選択を保持**し、次回起動時に復元する
6. **開始集中力**: できのよさの右側に表示。**ラベル「開始集中力」の縦位置は「できのよさ」ラベルと揃え**、数値はその下に中央配置(**基礎+針の内訳、開幕効果の記載は不要**)。針選択に連動
7. **はじめる**ボタン(大。プレイ画面へ遷移)

※フィルター・ソートに必要なレシピデータのフォーマット変更(必要職人レベル・装備可能レベル・誤差制限の追加)は **SPEC v1.23 で確定・実装済み**(2026-07-08。§7-9)。装備可能レベル「-」(非装備品)は「すべて」以外のフィルター値にマッチしない。モックの装備Lv・フィルター値は実値反映までダミーのまま。

### 5.8 ヘルプ(新設合意: 2026-07-08)

凡例(マス状態色の意味。D23)の移動先としてヘルプを新設する。収載内容・表示形式(独立画面かモーダルか)は画面遷移の設計時に確定。

### 5.9 スマホ版の新規設定項目(積み上げ)

§5 の判定から発生した、ユーザー変更可能な設定項目。設定画面の設計(§5.5 の判定後)への入力。

| # | 設定項目 | デフォルト | 由来 |
|---|---|---|---|
| C1 | マス基準値の表示 ON/OFF(表示時は小さく併記、非表示時は残り数値を大きく) | **OFF(非表示)** | D13 |
| C2 | 特技ボタンの配置順(5行4列内の並べ替え) | §5.3.1 の配置 | D25 |

### 5.10 画面遷移(暫定確定: 2026-07-08)

```
トップ(タイトル)
├─ あそぶ ──→ レシピ選択画面(レシピ・針・★、集中力内訳)
│              └→ プレイ画面 ──(しあげる)──→ 結果オーバーレイ
│                  ヘッダー: ◀もどる│レシピ名│(ログ/メニュー)    └ もう一度→同条件で新規ゲーム
├─ リプレイ ─→ リプレイ読込画面(専用画面。テキスト入力はここのみ)
│              └→ プレイ画面(リプレイモード: 1手進む解放・特技欄⇔ログのトグル)
├─ 設定(C1 マス基準値表示 / C2 特技配置)
└─ ヘルプ(凡例ほか)
```

- 行動ログ呼び出し(D29)とリプレイコピーの入口はヘッダー右端のアイコンを候補とし、モックで確定する。

## 6. 計算機モードと共有するUI部品(検討待ち)

計算機モード(W3)の仕様検討時に、§5 の確定結果と突き合わせて洗い出す。候補メモ: 布グリッド(表示+入力)、状態表示パネル、特技ボタン群、誤差評価値表示。

## 7. 未決事項

1. S4(タッチターゲット)・S5(素材解像度)のモック確認
2. サイクル表示での布特性予告の表現(§5.1.2。モック段階で比較・決定)
3. DQ10寄せ比較モックの作成と判断(§4.3)
4. ログ呼び出し・リプレイコピーの入口位置(§5.10。モックで確定)
5. 全パーツの画像AI生成(ユーザー担当。§4.6 のプロンプト一覧)と組込み。グループ②(アニメ系 fx_hissatsu)は生成テスト後に採否判断(§4.6.3。文字入りはフォント合成に確定済み)
6. **圧縮リプレイフォーマットの設計**(D38。SNS共有向け。既存のリプレイ形式(ARCHITECTURE A6)からの変換を別途設計)
7. プレイ中ヘッダー「◀もどる」の確認ダイアログ要否(進行中セッションの破棄を伴うため。モック段階で判断)
8. **管理者モードの仕様**(別途決定。決定済みの関連事項: 広告非表示+布グリッド拡大(D51)、シード入力の開放候補(S9))
9. ~~レシピデータのフォーマット修正~~ → **解決済み(2026-07-08)**: SPEC v1.23 で確定・実装済み(必要職人レベル・装備可能レベル(「-」=非装備品。フィルターで「すべて」以外に不一致)・誤差制限(0/1、§3.7 の制限あり評価境界))。既存データは暫定値(80/130、ぬいぐるみ「-」/0)のため、実値の反映待ちのみ残る。モックの装備Lv・フィルター値のダミー差し替えは実値反映後。
10. ~~実機レビュー残修正タスク R1〜R9~~ → **全件対応済み(2026-07-10。§4.8.1)**

## 8. 更新履歴

- v0.54 (2026-07-10): 実機レビュー再指摘(btn_skill系・btn_finish系の角の白残りが解消していない)への対処: ソース段階の白リム剥離だけでは列ごとの停止揺れ+縮小の滲みで半透明の白/明灰が外周(特に四隅)に残ることを実測で確認し、**最終出力解像度でのハロー除去**を加工スクリプトに追加(§4.8)。紺縁5点はハロー0に、選択(金)ボタンは純白のみ除去しクリーム〜淡金の縁を維持。6点を再加工。
- v0.53 (2026-07-10): レシピ選択画面モックをプレイ画面と同じ画像AI実素材+新スタイルに統一(モック v3): 背景=bg_main、ヘッダー/ツールボタン=btn_header、ウィンドウ=panel_window 9-slice、部位タブ・フィルター・針・できのよさのチップ=chip_on/off(背景100%伸縮)、布特性バッジ=badge_cloth_*(50×20固定幅)、はじめる=btn_primary 9-slice。レイアウト構造・JSは不変。選択行のハイライトは金CSSグラデを維持(横長行にカプセル素材の伸縮は不適)。
- v0.52 (2026-07-10): 実機レビュー追加対応: btn_skill系・btn_finish系の「角が透過されていない」指摘の原因は生成時に焼き込まれた**全周の純白アウトライン**(四隅では弧沿いに幅広・α≈1)と特定し、加工スクリプトに**白リム剥離**(透明領域に接する無彩色白の連鎖除去。§4.8)を追加して6点を再加工。旧版原本 assets-src/cell_*00.png ×3 をリポジトリから削除(assets-src/ng/ へ退避済み・git管理外)。
- v0.51 (2026-07-10): ぬいパワータイトルプレート(label_power)を「ぬいパワー」サイズ準拠の2行分の高さ(表示68×38)に変更し、**必殺使用時は「ぬいパワー/会心×２」の2行表示**(×２全角・上下左右センタリング)とする仕様を追加(D10・§4.4)。モック v2.6: プレートタップで必殺表示例を切替。
- v0.50 (2026-07-10): きらめきバッジの間隔を再調整: 素材がグロー(発光の裾)を含むため見た目の間隔が開いて見える指摘に対し、グローの重なりを許容して中心間距離を16px→12px(25%詰め)に変更(D5)。
- v0.49 (2026-07-10): 実機レビュー追加対応(続き): cell系の「白い角」は緑相ワイプ後も残存 — 原因は採用素材自体の角がほぼ直角で、角の外側に白系(上辺ハイライト由来)のAA画素が残るため。実装側で仕様の角丸 rx8@2x(表示4px)を border-radius クリップとして適用し、角の白残りを除去して四隅にグリッド布地が見えるようにした(§4.4 cell 行に記録。SVG素材の残存ではないことをモック全文の参照走査で確認済み)。
- v0.48 (2026-07-10): 実機レビュー追加対応: 素材の角丸外側の半透明ヘイズと緑残り(btn_skill_pressed 等)を解消 — 加工スクリプトに**緑相ワイプ**(色味比率判定。§4.8)を追加し12点(cell系5・btn_skill系4・btn_finish系2・icon_power)を再加工。精神統一きらめきの間隔を「左端基準・ほぼ接する1px」に変更(D5)。cell_ol_target の外周角が直角・不透明である旨の注意を §4.4 に記載(モック未使用。W1組込み時に確認)。
- v0.47 (2026-07-10): 実機レビュー追加調整: 数値・メッセージ領域の文字表示部一式(ターン/残り集中力/誤差+メッセージ欄)を「ターン」ラベル基準0.5文字(≒7px)右に移動(1手戻すボタンは特技4列目との整列を維持し不動。D50)。
- v0.46 (2026-07-10): **R9 完了(実機レビュータスク全件完了)**: ユーザー再生成の5点(cell_shitsuke=黄ティント強化 / cell_glow=全面桃色発光 / cell_shitsuke_glow=濃い合成 / btn_header 通常・押下=濃いこげ茶寄りの飴色)を採用確定し、再加工してモックへ組込み。
- v0.45 (2026-07-10): **実機レビュー修正 R1〜R8 を実施**(モック v2.5): 余白を画面下端へ(R1)、布特性バッジをCSS円形描画に戻し badge_trait_*.png を削除(R2)、サイクル表示できらめき非表示(R3)、グリッド内部を panel_grid の border-image fill で置換し旧CSS布地グラデを撤去(R4)、できのよさ★を3枠に修正(R5)、結果ボタン文字を縦中央に(R6・flexセンタリング)、ヘッダー「◀もどる」+◀縮小(R8)。加工スクリプト改修: GLOW_PARTS のトリムを✦除外+低しきい値+正方形センタークロップ化(きらめき正円化・星の先端保護。R3/R5)、btn_skill_selected に緑被りGクランプ(R7)。R9(素材再生成)のみ残。
- v0.44 (2026-07-10): モック v2.4 の実機レビュー反映: S2 を「余りの縦余白は画面下端に寄せる」に変更。布特性バッジは画像素材を廃止し実装側描画(SVG)に戻す(§4.4・§4.6.2)。きらめきバッジはコンパクト表示のみ(サイクル表示では布特性バッジと重なるため非表示。D5)。再生成プロンプト修正: cell_shitsuke(黄ティント4割)/cell_glow(全面発光)/cell_shitsuke_glow(濃く)/btn_header(濃い飴色)。ヘッダーは「◀もどる」(ひらがな統一・◀は小さめ。D39)。実装修正の残タスク R1〜R9 を §4.8.1 に起票(後日 sonnet 委譲)。
- v0.43 (2026-07-09): **画像AI素材42点を全面組込み**(モック v2.4)。§4.8 を組込み記録に全面改稿(透過化・トリム・リサイズの加工方式、9-slice スライス値、9-slice対象は縦横比保持)。panel_window の作成pxを 384×384(角96)に変更(結果窓の縁28px表示対応)。第1弾 art_* 素材とSVGプレースホルダを削除。結果画面の★を star_result 画像に、結果ボタンを btn_primary/btn_secondary 素材に置換。押下状態(:active)を全ボタンに配線。
- v0.42 (2026-07-09): 生成素材の格納レビューを反映: badge_sparkle は白発光の円形オーブに変更(採用素材に合わせる。D5・§4.4・§4.6.2)。cell_ol_target は濃紺枠で採用確定(プロンプト表現を修正)。cell_shitsuke のプロンプトに白破線の強調を追記(生成物に破線が出なかったため再生成)。fx_hissatsu は黒背景6コマ連番を加算合成前提で採用。再生成対象: panel_grid_regen/rainbow/light(v0.39プロンプト)、cell_shitsuke、cell_glow(桃色)、cell_shitsuke_glow(未生成)。
- v0.41 (2026-07-09): モックレビュー反映: badge_sparkle を布特性バッジと同サイズ15×15dp/30×30pxに変更(D5・§4.4・§4.6.2)。§5.1.2 を案1で確定(バッジ位置=ぬいパワープレートの右上。**再生バッジは4の倍数ターンのエントリに表示**する仕様変更 — SPEC §3.6 の発動タイミングは不変。虹・光は発動ターン表示のまま)。「×２」の黒縁を廃止(グラデ塗り+シャドウのみ)。布グリッドの状態色数字の縁を黒から濃い状態色(濃黄/濃緑/濃赤)に変更(§4.9)。
- v0.40 (2026-07-09): 生成運用からの設計変更: §4.9 新設(UIフォント=源暎ラテミン v2 の woff2 サブセット埋め込み、黒縁白抜き+ドロップシャドウの2レイヤー文字スタイル、白以外は縦グラデーション)。マス状態色(D14)を下地差分から**数値の文字色**表現に変更し cell を1種に統一。しつけがけ(D15)・発光(D16)は重ねオーバーレイをやめ**マスごと差し替え**(cell_shitsuke / cell_glow / 同時発生用 cell_shitsuke_glow。×２は全角・黄・数値の6割サイズでフォント合成)。ぬいパワーは共通ベージュプレート1種+文字色でパワー色を表現、「？」は全角(D2)。グループ①は48→42点(§4.4・§4.6.2)。
- v0.39 (2026-07-09): 生成テスト結果を反映: §4.6.1 に「個別プロンプトの色・縁指定は共通スタイルより優先」を追加。panel_grid_normal(紺の帯を禁止・細い金+白縁のみ・コーナー装飾1/6以内を強調)と cell_normal(細い焦茶1本線のみ・紺/金/コーナー装飾/白外縁の禁止)のプロンプトを修正 → 4色グリッド枠とマス素材は再生成。bg_main・panel_window は生成品を採用確定。
- v0.38 (2026-07-08): 未決事項9(レシピデータのフォーマット修正)を解決済みに更新(SPEC v1.23 で確定・実装。§5.7 の注記も更新)。
- v0.37 (2026-07-08): **日本語文字を画像に含めない方針を確定**(生成テストで文字品質が低かったため)。icon_power_×5・label_power を文字なしプレート+フォント合成に変更しグループ①へ移動(①42→48点)。グループ②は fx_hissatsu のみ(§4.4・§4.6・§4.7・§7-5)。logo_title も装飾画+フォント合成を基本に。
- v0.36 (2026-07-08): btn_header も木調をやめ「飴色の厚手布張り+縁沿いのステッチ」に変更(§4.4・§4.6.2)。
- v0.35 (2026-07-08): §4.6.1 共通スタイルから「木地・木彫の質感」を除去し「織り布・ステッチ(縫い目)の質感」に変更(ボタン類に木目が乗るのを防ぐ。裁縫アプリとして布系テイストは可)。
- v0.34 (2026-07-08): 素材方針を改定: SVGとの併用をやめ**§4.4 の全パーツを画像AI生成に統一**(質感差の解消。SVGは繋ぎのプレースホルダ)。§4.6 を全ID対応のプロンプト一覧に全面改稿(グループ①通常42点/グループ②文字・アニメ系7点=生成テスト後に採否判断。状態差分は別ID、最終px指定、単色背景・9-slice配慮の運用ルールつき)。
- v0.33 (2026-07-08): 画像AI生成素材の第1弾を組込み(§4.8): 背景=羊皮紙、結果ウィンドウ=金装飾枠、特技ボタン=状態差分3種+紺金(1手戻す)。原本と切り出し座標は assets-src/ に保管。
- v0.32 (2026-07-08): マス状態色をベタ塗りから「クリーム地+淡ティント18%+状態色インナーリング」に変更(彩度・塗り面積の抑制。数値も濃色に)。ヘッダーボタンの木目線を削除(§4.4)。
- v0.31 (2026-07-08): マス四隅の見え方を修正: 角丸との差分領域を、マス素材の下に敷いた布地レイヤー(布タイプの地色+斜線)で埋める方式に変更(§4.4)。
- v0.30 (2026-07-08): SVG試作のレビュー反映: 1手戻すボタンの幅・位置を特技ボタン4列目に揃える(D50)。布マスの角丸を縮小(rx18→8@2x)し四隅の背景見えを解消(§4.4)。
- v0.29 (2026-07-08): 素材の制作分担を決定(§4.7): 枠・ボタン・マス・バッジ類=SVG内製(解像度非依存)、絵画的パーツ=ユーザーの画像AI生成。ぬいパワー文字はプレート+フォント合成で確定。P1 の SVG 27点を試作しプレイ画面モックへ組込み(border-image による 9-slice)。
- v0.28 (2026-07-08): 素材の具体化(③): パーツ一覧24点(ID・サイズ@2x・状態差分・9-slice・優先度。§4.4)、命名・格納規約(§4.5)、生成プロンプト指針とぬいパワー文字の合成方式推奨(§4.6)を起票。
- v0.27 (2026-07-08): 結果オーバーレイのモック第1版を作成し、構成を §5.6 に記載(スクリム+中央ウィンドウ: できのよさ★・使用ターン数・残り集中力・リプレイコピー・もう一度、盤面確認用の1手戻す提案つき)。プレイ画面モックの「しあげる」から表示。
- v0.26 (2026-07-08): フィルターの選択肢をラベルと同一行に戻し、各行の「すべて」の横位置を揃えた。布特性バッジを固定幅+中央揃えに(§5.7)。
- v0.25 (2026-07-08): レシピ選択画面の再修正(§5.7): フィルターから必要職人レベルを削除、装備可能レベルの選択肢は「すべて」+実在Lv降順の横スクロール、ソート記号は小型三角に縮小、開始集中力ラベルの縦位置をできのよさラベルに揃え数値を下段中央へ。
- v0.24 (2026-07-08): レシピ選択画面のレビュー反映(§5.7): 部位順=頭→ぬいぐるみ、フィルター新設(たたみ・職人Lv/装備Lv/布特性)、ソート=装備Lv▼/▲、針は「さいほう針」表記で光→銅順、★は「できのよさ」表記、針・★のローカル保持、開始集中力はできのよさ行右側に数値のみ。レシピデータのフォーマット修正(職人Lv・装備Lv・誤差制限)を未決事項に追加。
- v0.23 (2026-07-08): レシピ選択画面のモック第1版を作成し、構成を §5.7 に記載(部位タブ+レシピ一覧(布タイプバッジ・伸縮吸収)+針/★選択+開始集中力内訳(針連動・開幕効果注記)+はじめる)。プレイ画面モックの「◀戻る」と相互遷移。
- v0.22 (2026-07-08): 数値行のラベル位置を固定(全数値に最大桁数分の幅を予約: ターン2桁/集中力3桁/誤差4桁)。左詰めをやめ、4項目を行幅に均等配分(D50)。
- v0.21 (2026-07-08): 数値行の間隔を8→16pxへ緩和(詰めすぎの修正)。誤差の数値は22px・4桁分の幅を常時予約し、桁数変化で動かないレイアウトに(D50)。
- v0.20 (2026-07-08): レシピ名フォントを16→20pxへ拡大(D47)。最大9文字が下限360dpで収まることを実測確認(ヘッダー左右余白を8→6pxに圧縮)。
- v0.19 (2026-07-08): 数値行の列間を圧縮(内容幅で左詰め+1手戻す右寄せ)し、数値フォントを22pxへ拡大(D50)。
- v0.18 (2026-07-08): 数値フォントを拡大(18→20px)。誤差4桁+下限幅360dpで崩れない上限として実測決定(D50)。
- v0.17 (2026-07-08): 広告領域をアンカーバナー前提に具体化(D46): 全幅・高さ可変(50〜90dp目安、SDK取得値で確保し余白で吸収)・上部配置の根拠(特技ボタンからの分離=誤タップ防止)を記録。
- v0.16 (2026-07-08): 数値行の揃えを追記(D50): ラベルと数値の下端揃え、「残り」は「集中力」と左端揃え。
- v0.15 (2026-07-08): 集中力ラベルを「残り/集中力」の2行表記に、数値行は高く・メッセージ行は低く(D50)。管理者モード時は広告非表示+布グリッド拡大(D51。管理者モード仕様は別途決定、未決事項に登録)。
- v0.14 (2026-07-08): モックv2.1レビュー反映: パワーアイコンは現在/次以降で同一サイズ、特技使用時のスライドモーションを追加(D3)。精神統一は残りターン数と同数の「きらめき」縦長楕円バッジに変更(D5、淡青発光案は廃止)。メッセージは欄内センタリング(D49)。ぬいパワーシフトのみ特技名2行の3行レイアウト(§5.3.1、ボタン幅の均等化)。
- v0.13 (2026-07-08): D45 の詳細レイアウトを確定: 数値・メッセージ領域は2行×4列グリッド(1行目=ターン/集中力/誤差、2行目1〜3列=メッセージ欄、4列目=1手戻す縦2行分)。行間に1〜3列幅の罫線。メッセージ欄はウィンドウ調にしない。「!」は全角。
- v0.12 (2026-07-08): モック第1版のレビュー反映: **トーンBで決定**(§4.3)。次パワー予告は3つ先まで(D3)。サイクル全体表示は横スクロール・T番号なし・高さ縮小(§5.1.1/5.1.2)。追加表示要素(§5.6.5): メッセージ欄D45(文言表つき)・広告領域D46・ヘッダー整形D47・グリッド縦縮小許容D48。数値表示は「ターン・集中力」と「誤差」の2行に分割。
- v0.11 (2026-07-08): S1〜S10 を決定(S4・S5 はモックで確認。S2=上下余白を伸ばし中央寄せ、S9=シード入力不可(管理者モード開放は将来検討)・テキスト入力はリプレイ専用画面のみ)。画面遷移を暫定確定(§5.10)。
- v0.10 (2026-07-08): 5.6 を確定(結果画面はオーバーレイ、誤差内訳表は不要、リプレイコピーあり(圧縮)、「もう一度」はレシピ・針そのままで新規ゲーム)。これで表示項目インベントリ D1〜D44 の判定がすべて完了。
- v0.9 (2026-07-08): 5.5 を確定: レシピ・針選択は開始前画面へ、予告ON/OFF・検証モードトグル・シードは削除、1手戻すは常設・1手進むはリプレイモード限定、リプレイコピーは圧縮フォーマットを別途設計、読込はリプレイモード入り口で実施、プレイ画面にヘッダー新設(左「◀戻る」・中央レシピ名)。
- v0.8 (2026-07-08): 5.4 を確定: D29=ログは必要時に呼び出す別画面、D30=ハイライトはリプレイモードで使用、D31=出目は表示・会心率は削除。**スマホ版は検証モードを廃止しリプレイモードを新設**する方針を追加(§5.4.1。特技欄とトグルのログ表示方式を検討)。
- v0.7 (2026-07-08): 5.2 の残り(D12・D14〜D19・D24)を推奨案どおり確定。5.3 を確定: D25=特技ボタン5行4列・配置順を設定化(C2)・デフォルト配置は §5.3.1(列優先で系統別)、D28=しあげるの専用誤タップ対策なし(1手戻すで復帰)。
- v0.6 (2026-07-08): 5.2 の一部判定を確定: D13=基準値表示を設定化(デフォルト非表示。新規設定 C1)、D21=誤差はターン・集中力と同一行で文言「誤差」、D22=★3ライン不要、D23=凡例はヘルプ新設(§5.8)へ移動。新規設定項目の積み上げ表(§5.9)を追加。
- v0.5 (2026-07-08): 必殺チャージ保持中の表現を確定: 「無我の境地」ボタンの有効/無効で表す(チャージの効果は無我の境地の実行可否のみ)。
- v0.4 (2026-07-08): D10 の判定を訂正(常時の状態バッジは×)。「画像+エフェクト」の決定は D20(必殺チャージ演出)へ移し ◎ で先行確定。
- v0.3 (2026-07-08): サイクル切り替えは案A(パワー行のその場トグル)に決定(問題があれば再検討)。布特性予告はブラウザ版の下線をスマホ版では変更することを決定し、候補(特性アイコンバッジ+ターン番号 / 色枠・グロー)を追加(§5.1.2、モックで比較)。
- v0.2 (2026-07-08): §5.1(D1〜D11)の判定確定(パワー表示は画像化・予告あり固定・アイコン並び、精神統一は淡青の発光表現、集中力バー/布特性表示/無我バッジ廃止、D8はレシピ選択画面へ)。レシピ選択画面の新設合意(§5.7)。サイクル切り替え方式の候補を追加(§5.1.1)。
- v0.1 (2026-07-08): 初版。M5開始。決定済み前提(縦持ち固定・生成画像・操作系踏襲・ゼロベース確認)、画面前提の論点表 S1〜S10、素材のパーツ分割観点と権利面の線引き、表示項目インベントリ D1〜D44(推奨案つき・判定未)。
